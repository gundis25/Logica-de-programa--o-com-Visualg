Algoritmo "teste_para3"

Var
  I, N, X, soma : inteiro

Inicio
   escreva("Quantos numeros serao digitados? ")
   leia(N)
   soma <- 0

   para I de 1 ate N faca
      escreva("Digite um nomero: ")
      leia(X)
      soma <- soma + x
       

   fimpara
       escreva("SOMA= ",soma)

Fimalgoritmo